# chroot-escape

Escape chroot sandbox and execute command from the original root.

## Build from source

Generate autotools scripts

```sh
sh autogen.sh
```

Configure

```sh
./configure --enable-maintainer-mode
```

Build

```sh
make
```

Install as root

```sh
make install
```
